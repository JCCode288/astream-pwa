import { AnimeProviderInterceptor } from './anime-provider.interceptor';

describe('AnimeProviderInterceptor', () => {
  it('should be defined', () => {
    expect(new AnimeProviderInterceptor()).toBeDefined();
  });
});
